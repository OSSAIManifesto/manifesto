## Key Rules

- `src/app/page.tsx` is the source of truth for principles. When modifying principles there, copy the exact titles and descriptions verbatim into `public/llms.txt` to keep them in sync.
